{
  "name": "opencode-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "本地 stdio MCP 桥：让 Claude Desktop / Cursor / Codex 通过本地路径调用远程 opencode-api 识图",
  "bin": {
    "opencode-mcp-bridge": "index.mjs"
  },
  "scripts": {
    "test": "node --test ./mcp-bridge/lib.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
